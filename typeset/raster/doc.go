// Package raster is a page of colored text cells, one byte per cell:
// panels of rows by columns in a geometry the caller chooses, a fixed
// cell repertoire, in-band row-scoped ink, a line-oriented authoring
// language that compiles to the bytes, and renderers to plain text,
// ANSI and HTML. The specification is RASTER.t, embedded and returned
// by Spec; tessera (repani.com/tessera) is the first geometry.
package raster
